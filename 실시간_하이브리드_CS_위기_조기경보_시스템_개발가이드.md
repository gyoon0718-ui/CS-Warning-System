# 🚀 실시간 하이브리드 CS 위기 조기 경보 시스템 — 최종 개발 가이드

---

## Phase 1: 데이터 전처리 및 AI 모델 학습 (Offline / Colab)

> 가장 먼저 기반이 되는 **'텍스트 감정 분석 AI'의 두뇌**를 만드는 작업입니다.  
> 팀의 데이터/AI 파트가 주도합니다.

### 1-1. 데이터 추출 및 정제
- 파이썬(Pandas)을 이용해 AI Hub의 JSON 파일에서 **'발화 텍스트'** 와 **'감정 라벨'** 만 추출하여 CSV로 저장합니다.

### 1-2. 라벨 범주화 및 맵핑
- 수많은 감정 라벨을 **4가지(기쁨, 중립, 슬픔, 분노)** 로 통폐합합니다.

### 1-3. 딥러닝 모델 파인튜닝
- Google Colab의 무료 GPU를 활용해 Hugging Face의 **KcELECTRA** 모델에 정제된 CSV 데이터를 학습시킵니다.

### 1-4. 모델 가중치 추출
- 학습이 완료된 모델의 파일(가중치)을 다운로드하여 **로컬 프로젝트 폴더**로 가져옵니다.

---

## Phase 2: 오디오 파이프라인 및 백엔드 로직 (Backend / Python)

> 오디오 파일을 입력받아 **텍스트 변환 → 물리량 추출 → 시계열 위험도 계산**까지 수행하는 백엔드 엔진을 구축합니다.

### 2-1. STT 변환 모듈 구축
- Whisper API(또는 로컬 모델)를 연결하여 고객의 `.wav` 오디오 파일을 **텍스트로 변환**하는 파이썬 함수를 작성합니다.

### 2-2. 오디오 물리량 추출
- **Librosa** 라이브러리를 활용해 동일한 오디오 파일에서 목소리 크기(데시벨/RMS)와 말하기 속도(Tempo) 수치를 뽑아내는 함수를 만듭니다.

### 2-3. 하이브리드 점수 산출
- STT로 뽑은 텍스트를 Phase 1의 감정 모델에 넣어 **기본 점수**를 구하고, 물리량(크기/속도)이 비정상적으로 높을 경우 **감점(Penalty)** 을 부여해 최종 턴 점수를 확정합니다.

### 2-4. 시계열 및 임계점 로직
- 현재 턴의 점수를 기존 누적 점수에 합산하고, 직전 턴 대비 **하락 기울기**를 계산하여 대화의 위험 상태(**안전 / 주의 / 위험**)를 판별하는 최종 룰셋 모듈을 완성합니다.

---

## Phase 3: 대시보드 UI 및 프론트엔드 연동 (Frontend / Streamlit)

> 백엔드 로직의 결과를 **시각적으로 보여주는 화면**을 만듭니다.  
> 난이도 조절을 위해 **'파일 업로드(MVP)'** 방식으로 먼저 구현합니다.

### 3-1. 화면 레이아웃 분할
- Streamlit의 `st.columns`를 활용해 화면 **좌측**(오디오 업로드 및 대화록)과 **우측**(시계열 그래프 및 경고판)을 명확히 나눕니다.

### 3-2. 세션 상태 관리
- `st.session_state`를 세팅하여 대화 턴이 추가될 때마다 초기화되지 않고 **데이터프레임에 누적**되도록 만듭니다.

### 3-3. 실시간 시각화
- **Plotly** 또는 **Altair** 라이브러리를 사용해 누적된 데이터프레임을 기반으로 꺾은선 그래프를 그립니다.

### 3-4. 동적 UI/UX 연동
- Phase 2 모듈에서 **'경고'** 판정이 넘어오면 Streamlit 화면에 붉은색 알림 팝업과 함께 **[상담원 교체 권고]** 메시지가 뜨도록 연결합니다.

---

## Phase 4: 통합 테스트 및 고도화 (QA & Portfolio)

> 개발된 모듈들을 하나로 합치고, **실제 서비스처럼 다듬어** 포트폴리오로 완성합니다.

### 4-1. 엣지 케이스(예외) 테스트
- _'소리는 엄청 크지만 내용은 감사합니다 인 경우'_, _'조용조용하게 심한 욕설을 하는 경우'_ 등 다양한 오디오 샘플을 넣어 하이브리드 로직의 맹점을 보완합니다.

### 4-2. 처리 속도 최적화
- STT 변환과 Librosa 오디오 분석이 **동시에 실행**되도록 비동기/병렬 처리 코드를 적용하여 분석 지연 시간을 최소화합니다.

### 4-3. 문서화 및 아키텍처 정리
- GitHub에 깔끔한 `README.md`를 작성하고, **Mermaid로 그린 아키텍처 다이어그램**과 실제 구동되는 데모 영상(`.gif`)을 첨부하여 마무리합니다.

---

## 📅 기대 효과

> 이 가이드라인을 토대로 **4주간의 스프린트**를 진행하신다면, 단순한 데이터 분석을 넘어  
> **엔지니어링 파이프라인 구축**, **AI 모델 서빙**, **프론트엔드 연동**, **시계열 분석 역량**을  
> 모두 입증할 수 있는 **압도적인 포트폴리오**가 탄생할 것입니다.  
>  
> 프로젝트의 성공적인 완주를 응원합니다! 🎉
